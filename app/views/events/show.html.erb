<%= form_with model: @event, data: { turbo: false} do |f| %>
  <%= f.label :attendees, @event.title %>
  <%= f.check_box :attendees, { checked: true }, current_user.id, 'no'  %>
  <%= f.submit "Attend" %>
<% end %>
